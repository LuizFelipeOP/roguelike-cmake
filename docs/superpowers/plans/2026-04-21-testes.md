# Plano de Implementação — Infraestrutura de Testes

**Data:** 2026-04-21  
**Spec:** `docs/superpowers/specs/2026-04-21-testes.md`  
**Status:** Em execução

---

## Chunk 1 — Infraestrutura CMake

**Arquivo:** `CMakeLists.txt`

- Adicionar bloco `FetchContent` para doctest v2.4.11
- Criar `file(GLOB_RECURSE TEST_GAME_SOURCES ...)` excluindo `src/main.cpp` com `list(FILTER ... EXCLUDE REGEX)`
- Criar `file(GLOB TEST_SOURCES "tests/*.cpp")`
- `add_executable(roguelike_tests ...)`
- `target_include_directories` e `target_link_libraries`
- `enable_testing()` + `include(doctest)` + `doctest_discover_tests`

---

## Chunk 2 — Modificações em código de produção

### `Game.hpp`
- `render()` → `virtual void render()`
- `descerAndar()` e `inicializarAndar()` → mover para seção `protected`
- Adicionar getters protected: `getAndarAtual()`, `getNumEnemies()`, `getNumItems()`

### `Player.cpp`
- `adicionarObserver()` → adicionar guard contra duplicata de ponteiro

---

## Chunk 3 — Arquivo de testes: entidades

**Arquivo:** `tests/test_entities.cpp`

Casos:
- `Player` atributos iniciais
- `Player::takeDamage` com e sem mitigação
- `Player::curar` com e sem overflow de maxHp
- `Player::setPosition`
- `Player::addXP` e level up
- `Player::raiseAttack`, `raiseDefense`
- `Player::isAlive`
- `Goblin` scaling por andar
- `Troll` scaling por andar
- `Enemy::takeDamage` e `isAlive`

---

## Chunk 4 — Arquivo de testes: factories

**Arquivo:** `tests/test_factories.cpp`

Casos:
- `EnemyFactory::create` Goblin
- `EnemyFactory::create` Troll
- `ItemFactory::create` não-nulo
- `ItemFactory` andar 1 sem equipment
- `ItemFactory` andar 3 pode gerar Espada
- `ItemFactory` andar 5 pode gerar itens raros

---

## Chunk 5 — Arquivo de testes: mapa

**Arquivo:** `tests/test_map.cpp`

Casos:
- `Room::center`
- `Room::contains`
- `Room::intersects`
- `Map::generate` produz salas
- `Map::isWalkable` parede vs chão
- `Map::getTile` out-of-bounds
- `Map::getPosicaoEscada` tile `>`
- Fog of War antes e depois de `updateVisibility`

---

## Chunk 6 — Arquivo de testes: items

**Arquivo:** `tests/test_items.cpp`

Casos:
- `CuraStrategy` cura com e sem overflow
- `BonusDefStrategy` aumenta defesa
- `AumentarATKStrategy` aumenta ataque
- `Inventario::adicionarItem` consumível e equipamento
- `Inventario::cheio`
- `Inventario::equipar` e `desequipar`
- `Inventario::removerConsumivel`

---

## Chunk 7 — Arquivo de testes: observers

**Arquivo:** `tests/test_observers.cpp`

Casos:
- `StatsObserver` sem equipamento (sem bonus)
- `StatsObserver` com arma
- `StatsObserver` com armadura + acessório
- `StatsObserver` combinação completa
- Anti-duplicação de observer

---

## Chunk 8 — Arquivo de testes: integração Game

**Arquivo:** `tests/test_game.cpp`

Subclasse `GameTeste` com `render()` noop.

Casos:
- Andar inicial = 1
- `descerAndar()` incrementa andar
- Dois `descerAndar()` consecutivos

---

## Chunk 9 — Build e verificação

```bash
cmake -S . -B build
cmake --build build --target roguelike_tests
cd build && ctest --output-on-failure
```

Todos os testes devem passar com 0 falhas.
